- Aggiungere priorità attività

- Aggiungere all'apertura un controllo di versione del programma. se la versione combacia allora non fare niente, ma se non combacia far partira una classe con la lista degli aggiornamenti automatici e vari controlli da far eseguire automaticamente dal programma.

[Fatto] - Aggiungere alle cartelle la possibilità di impostare che tipi cartella possono contenere. La finFolder potrà selezionare solo quelli in base al tipo scelto.

[Fatto] - Aggiungere un progressivo nel JSON della cartella di 3 cifre da concatenare all'inizio della cartella attività in modo da far seguire un ordine di creazione. Nel programma il contatore non verrà visto ma sarà presente per poter utilizzare il percorso.
- Alla creazione dell'attività impostarla con stato "Nuovo". Creare poi una funzione per la modifica dello stato inserendo lo stato, in caso di chiusura la data di chiusura e in caso di rilascio la data di rilascio, delle note. Queste informazioni verranno scritte nel JSON dell'attività.
  Oppure ad ogni cambio di stato, aggiungere nel JSON dell'attività il nuovo stato, la data e l'ora di cambio stato, eventuali note.

- Gli stati andranno parametrizzati nelle opzioni.

- In modifica/elimina attività, aggiungere nella ricerca del percorso una combo con la possibilità di filtrare per cliente. Aggiungere tra le colonne anche il cliente a cui fa riferimento la cartella. Per i clienti tenere vuota la colonna e disabilitato il filtro della combo.

[Fatto] - Nella finCartella, allungare la colonna del nome in modo automatico per la massima lunghezza del nome più lungo in modo da vedere tutti i caratteri.

[Fatto] - Aggiungere le icone al programma.

- Per i parametri nelle opzioni, inserire una cella all'inizio con una combo in modo da poter selezionare il programma a cui fa riferimento. In questo modo tenere nel treeView solo "Parametri" senza l'elenco dei programmi.

- Creare una funzione per la gestione delle note.

- Nella generazione di una attività, specificare quali file creare, mettendo dei check su quelli comuni e una text box con nome e una con una combo editabile con le estensioni. due frecce bilaterali che vanno in un list box.

- Una volta che l'attività è stata chiusa o rilasciata non sarà più possibile modificarlo sia il contenuto che la cartella in se

- Aggiungere una funzione di "Esegui attività" dove selezionando il cliente, e poi l'attività verranno mostrate tutte le funzioni utili per l'esecuzione dell'attività. Una griglia con l'elenco dei file all'interno, un pulsante che rimanda alle note, la possibilità di aggiungere nuovi file all'interno, la possibilità di aprire i file con un particolare sistema di edit in base all'estensione.

- Alla chiusura di un'attività specificare l'intervento eseguito obbligatorio. Questo ovviamente se non è stata cancellata l'attività

[Fatto] - Quando si chiude il programma provare a metterlo tra i programmi in basso a destra tra le icone di notifica

- Aggiungere una finestra per l'elenco delle attività aperte in modo da raggiungerle subito, magari inserendo nella finCartella anche una colonna stato e filtrare per quelli aperti.